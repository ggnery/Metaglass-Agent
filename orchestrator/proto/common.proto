syntax = "proto3";

package metaglass;

// ── Standardized status ────────────────────────────────────────────
enum StatusCode {
  STATUS_CODE_UNSPECIFIED = 0;
  STATUS_CODE_OK = 1;
  STATUS_CODE_ERROR = 2;
  STATUS_CODE_PARTIAL = 3;            // response still in progress
  STATUS_CODE_RATE_LIMITED = 4;
  STATUS_CODE_AGENT_UNAVAILABLE = 5;
}

message Status {
  StatusCode code = 1;
  string message = 2;
}

// ── Media modality ─────────────────────────────────────────────────
enum Modality {
  MODALITY_UNSPECIFIED = 0;
  MODALITY_AUDIO = 1;
  MODALITY_IMAGE = 2;
  MODALITY_VIDEO = 3;
  MODALITY_TEXT = 4;
}

// ── Generic multimodal payload ─────────────────────────────────────
message MediaPayload {
  Modality modality = 1;
  bytes data = 2;                     // raw bytes (opus audio, jpeg frame, etc.)
  string mime_type = 3;               // "audio/opus", "image/jpeg", "video/h264"
  optional int64 duration_ms = 4;     // relevant for audio/video
  optional int32 sample_rate = 5;     // relevant for audio
  optional Resolution resolution = 6; // relevant for image/video
}

message Resolution {
  int32 width = 1;
  int32 height = 2;
}

// ── Streaming control ──────────────────────────────────────────────
message StreamControl {
  bool is_final_chunk = 1;            // true = last chunk in the stream
  int32 sequence_number = 2;          // for reordering on the orchestrator side
  string stream_id = 3;              // groups chunks belonging to the same stream
}

// ── Session state ──────────────────────────────────────────────────
enum SessionState {
  SESSION_STATE_UNSPECIFIED = 0;
  SESSION_STATE_ACTIVE = 1;
  SESSION_STATE_LOST = 2;
  SESSION_STATE_CLOSED = 3;
}
